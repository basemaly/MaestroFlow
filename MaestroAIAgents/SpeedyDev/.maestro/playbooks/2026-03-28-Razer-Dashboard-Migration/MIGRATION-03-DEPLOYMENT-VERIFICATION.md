# Migration Phase 3: Deployment & Security
Finalize the orchestration and access control.

- [x] Rebuild `docker-compose.yml` for Node/Hono/Nginx stack
- [x] Configure Nginx reverse proxy for SSL and API routing
- [ ] Implement Caddy config for secondary access control
- [ ] Automate `nftables` MAC filter regeneration in `setup.sh`
- [ ] Perform a full stack verification (status checks, controls, security)
